-- RC Carteira PJ A.A

VAR vDataAtual = MAX('DWH CALENDARIO'[Data])

VAR vTrimestreAnterior =
    DATESINPERIOD( 'DWH CALENDARIO'[Data], EDATE(vDataAtual, -12),  -3, MONTH)

RETURN

CALCULATE([RC Carteira PJ],vTrimestreAnterior)