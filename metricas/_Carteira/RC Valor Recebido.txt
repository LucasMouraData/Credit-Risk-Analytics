-- RC Valor Recebido

VAR vDataAtual = MAX('DWH CALENDARIO'[Data])

VAR vTrimestre = DATESINPERIOD( 'DWH CALENDARIO'[Data], vDataAtual, -3, MONTH)

RETURN
//CALCULATE([RC Contratos], vTrimestre)

CALCULATE(SUM('DWH VW_PARCELAS'[VALOR PAGO]),vTrimestre)